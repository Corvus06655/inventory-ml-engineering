# Notebooks

The original exploratory notebooks are kept in the local project ZIP because they contain large, dataset-specific exploratory output. The reproducible Python training and inference code is in `src/`, `scripts/`, `app/`, and `tests/`.

See the project README for setup and reproduction steps.
